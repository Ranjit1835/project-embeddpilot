/* EmbeddPilot compile-judge STUB — not the real FreeRTOS task.h.
 * Declarations only; see ../esp_err.h for the rationale. */
#ifndef EMBEDDPILOT_STUB_FREERTOS_TASK_H
#define EMBEDDPILOT_STUB_FREERTOS_TASK_H

#include "FreeRTOS.h"

void vTaskDelay(TickType_t xTicksToDelay);

#endif
