[project]
name = "embeddpilot"
version = "0.1.0"
description = "Datasheet to verified peripheral driver, automatically."
requires-python = ">=3.11"
dependencies = [
    "jsonschema>=4.20.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.4.0",
]
