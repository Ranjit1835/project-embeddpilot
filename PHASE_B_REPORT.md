# PHASE B — TRUTH PASS Report

> EmbeddPilot V1.1 — Every claim and behavior made honest.

**Date:** 2026-07-02
**Verdict:** PASS

---

## STEP 0 — CLAUDE.md (Orchestrator, pre-lane)

Fixed the file that poisons every agent context:
- `Sensor: BMP280 (virtual)` → `Sensor: BMP180 (virtual, I2C address 0x77, chip ID 0x55)`
- Added Arduino (Wire library) + PlatformIO to target spec
- `"The generator is NEVER the judge"` → `"PRINCIPLE — currently violated (Finding 2), restoration scheduled Phase C"`
- Added ground truth directive: `"FINDINGS.md is ground truth. When any doc conflicts with FINDINGS.md, trust FINDINGS.md."`
- Removed "Self-correcting loop" from build order, replaced with "Simulation verification"
- Added `KNOWN GAP` callout for IR not driving executable code (Finding 4)

---

## LANE 1 — PIPELINE CODE

**Files changed:** `embeddpilot.py`, `scripts/self_correct.py`

| Change | Detail |
|--------|--------|
| Removed multi-attempt loop | `for attempt in range(1, max_retries + 1)` → single pass: generate once, compile once, simulate with optional single infra retry |
| `--max-retries` removed | Replaced with `--sim-retry` / `--no-sim-retry` (boolean, default: enabled) |
| `self_correct.py` gutted | Now a 12-line deprecated wrapper that imports `embeddpilot.main()` |
| Build-injection preserved | `run_compile()` still does `shutil.copy2(driver_path, harness_src)` with backup/restore in `finally` block |
| Failure report | `write_failure_report()` writes `artifacts/failure_report.md` on any `[FAIL]` — includes failed test names, assertion text, raw serial output |
| Error handling | `TimeoutExpired` → COMPILE_TIMEOUT / SIM_TIMEOUT; `FileNotFoundError` → actionable install messages for pio and wokwi-cli |
| Failure detection broadened | Any `[FAIL]` line = test failure (no retry); nonzero exit + no HARNESS_COMPLETE + no `[FAIL]` = infra failure (eligible for single retry) |
| `WOKWI_CLI_PATH` env var | Replaces hardcoded `Path.home() / ".wokwi" / "bin" / "wokwi-cli.exe"` fallback |
| All prints reworded | No "self-correcting", no "AI-generated at runtime"; banner says "EmbeddPilot V1.1 Pipeline"; proof says "V1.1 — Verification Proof" |
| Steps renumbered | 1=validate IR, 2=generate, 3=compile, 4=simulate, 5=package (TOTAL_STEPS=5) |

---

## LANE 2 — DOCS

**Files changed:** `README.md`, `TARGET.md`, `DONE.md`, `harness/NOTES.md`, `harness/SETUP.md`

| File | Changes |
|------|---------|
| `README.md` | "AI-powered pipeline" → "deterministic template engine, designed and built with Claude Code"; ESP-IDF → Arduino/Wire + PlatformIO; BMP280 → BMP180 (0x77); "Self-Correcting Loop" → "Simulation Verification"; Key Principle rewritten to acknowledge Phase C gap; "AI-generated drivers" → "Generated drivers (deterministic template)" |
| `TARGET.md` | "ESP-IDF framework" → "Arduino framework (Wire library), PlatformIO build system" |
| `DONE.md` | Complete rewrite: "Verified Truths" (7 items, all demonstrable) + "Known Gaps" (judge independence → Phase C, IR-driven code → Phase D, LLM-at-runtime → Phase E) |
| `harness/NOTES.md` | 0x76→0x77, BMP280→BMP180, chip ID 0x58→0x55, 6 bytes→3 bytes |
| `harness/SETUP.md` | Expected Serial Output rewritten: BMP180 target, chip_id=0x55, ctrl_meas=0x20, raw_temp=29028, post-reset chip_id OK |

---

## LANE 3 — TEMPLATE STRINGS

**File changed:** `codegen/driver_gen.py` (string/comment level only)

| Line | Old | New |
|------|-----|-----|
| 43 | `AI-Generated I2C Driver` | `Generated by EmbeddPilot (deterministic template)` |
| 49 | `IR-derived configuration notes:` | `IR-derived notes (comments only — IR does not yet drive executable code, see Phase D):` |
| 53 | `All register knowledge comes from the validated IR.` | `Sensor constants: template-defined. IR-grounded generation arrives in Phase D.` |
| 70 | `I2C Pin Configuration (from IR: ...)` | `I2C Pin Configuration (template-defined for ...)` |
| docstring | `Includes the auto-generated register header for documentation/validation` | `Generates standalone driver (does not yet #include the register header — Phase D)` |
| docstring | `The IR provides the I2C controller register knowledge...` | `The IR provides comment-level metadata. Executable code uses template-defined sensor constants via the Arduino Wire library.` |

No logic, structure, or test function changes.

---

## LANE 4 — APP STRINGS

**File changed:** `app.py` (string level only)

| Change | Detail |
|--------|--------|
| Header | "AI pipeline" → "Deterministic template engine...Designed and built with Claude Code" |
| Module docstring | "AI pipeline" → "deterministic template engine" |
| Exit criteria | Removed "Self-correction" item; updated Codegen and Verification descriptions; list now 6 items |
| Known Gaps section | Added subheader + two `st.warning` items (judge independence → Phase C, IR-driven code → Phase D) |
| Driver tab title | "AI-Generated Driver" → "Generated Driver" |
| Footer | Verified "Designed and built with Claude Code" |
| Proof tab | Added support for new V1.1 report format (`steps` dict) alongside old `iterations` list |

---

## ORCHESTRATOR FIX

**File:** `harness/src/main.cpp` (reference driver comment)
- `"The AI-generated driver must produce identical"` → `"The generated driver must produce identical"`

---

## VERIFICATION RESULTS

### Run 1 — Full Pipeline (correct config)

```
VERDICT: PASS
Timestamp: 2026-07-02T03:08:28.194084+00:00
Steps:
  validate_ir: PASS
  generate: PASS
  compile: PASS
  simulate: 5 passed, 0 failed
    [PASS] test_i2c_init
    [PASS] test_chip_id
    [PASS] test_write_read_config
    [PASS] test_burst_read
    [PASS] test_soft_reset
FINAL VERDICT: PASS
```

### Run 2 — Forced Failure (chip_id_value: 0x55 → 0xAA)

```
VERDICT: SIM_FAIL
Timestamp: 2026-07-02T03:07:41.147968+00:00
Steps:
  validate_ir: PASS
  generate: PASS
  compile: PASS
  simulate: 3 passed, 2 failed
    [PASS] test_i2c_init
    [FAIL] test_chip_id  (expected=0xAA, got=0x55)
    [PASS] test_write_read_config
    [PASS] test_burst_read
    [FAIL] test_soft_reset  (post-reset chip_id=0x55, expected 0xAA)
FINAL VERDICT: SIM_FAIL

artifacts/failure_report.md written with:
  - Failed test names
  - Assertion text
  - Raw serial output (ESP32 boot + harness output)
```

### Run 3 — Post-Revert Confirmation

```
VERDICT: PASS (same as Run 1, 5/5)
```

---

## GREP AUDIT — Banned Terms

| Pattern | Live hits (excluding FINDINGS.md) |
|---------|----------------------------------|
| `BMP280` | **0** |
| `0x76` | **0** (harness/reference/driver_broken.cpp: intentional bug, exempt; scripts/generate_driver.py: validation whitelist, exempt) |
| `self-correct` (case-insensitive) | **0** |
| `AI-generated` (case-insensitive) | **0** |

All banned terms eliminated from live code and documentation. Only FINDINGS.md (historical audit, exempt) retains references.

---

## SUMMARY

Phase B made every claim in the codebase honest:
- **11 BMP280→BMP180 contradictions**: all fixed, zero remaining
- **"AI pipeline" framing**: replaced with "deterministic template engine" everywhere
- **"Self-correcting loop"**: removed from pipeline, docs, and UI
- **"Generator is never the judge"**: reworded to acknowledge the current violation, with Phase C plan
- **IR gap**: documented honestly in CLAUDE.md, driver comments, app.py, DONE.md
- **Pipeline hardened**: single-pass with infra retry, proper error handling, failure reports
- **All 3 runs verified**: PASS → forced SIM_FAIL → PASS again
